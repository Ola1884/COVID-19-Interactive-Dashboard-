{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": gunicorn app:server --bind 0.0.0.0:$PORT --timeout 300 --preload --workers 1,
    "restartPolicyType": "ON_FAILURE"
  }
}
